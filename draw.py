
import random
import os.path
import time

from PIL import Image, ImageDraw, ImageFont

class glyph:

    name = ""
    width = 0
    height = 0

    def from_file(file_name):
        #load from __file__
        return

    def render_to(target_bytes):
        #render it to an array?
        return

class glyph_font(glyph):
    chars = {}

    def __init__(w, h, file_name):
        return

    def from_file():
        #super from_file()
        # confirm sizes and such
        return
